initialiseDate = () => {
  var elems = document.querySelectorAll(".datepicker");
  // var default_date = document.getElementById("default-date")
  //   ? document.getElementById("default-date")
  //   : null;

  if (elems) {
    /*var instances = M.Datepicker.init(elems, {
      onClose: () => {
        for (let i = 0; i < elems.length; i++) {
          const element = elems[i],
            { id, value } = element;

          this.setState({
            [id]: value
          });
        }

        this.setState({
          age: this.calculateAge(this.state.dob)
        });
      },
      yearRange: 50,
      format: "yyyy-mm-dd",
      setDefaultDate: true,
      defaultDate: new Date(default_date)
    });*/
  } // if
};